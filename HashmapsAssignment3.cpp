#include<iostream>
#include<vector>
#include<string>
#include<unordered_map>
using namespace std;
vector<string>UniqueCharacter(char* str,int size){
    vector<char>output;
    unordered_map<char,bool>m;
    for(int i=0;i<size;i++){
        if(m[str[i]])
    }
}